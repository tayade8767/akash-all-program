#include <iostream>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--)
	{
	    int n,a,b,count=0,i;
	    cin>>n>>a>>b;
	    for(i=1;i<n;i*=2)
	    {
	        count++;
	       //cout<<i<<" ";
	    }
       // cout<<count
	    cout<<(count*a)+((count-1)*b)<<endl;
	}
	return 0;
}
