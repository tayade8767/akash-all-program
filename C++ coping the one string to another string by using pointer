#include <iostream>
#define max 100
using namespace std;
int main() 
{
   char a[max],b[max];
   char *ptr1=a;
      char *ptr2=b;
      
   cout<<"enter the string"<<endl;
   cin>>a;
 
   while(*(ptr2++)=*(ptr1++));
   
   cout<<"enter the string   "<<a<<endl;
      cout<<"enter the string   "<<b<<endl;

    //cout<<*ptr1;
    return 0;
}
