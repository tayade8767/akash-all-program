#include<iostream>
#include<stack>
using namespace std;
int main()
{
    deque<int> v;
    v.push_back(12);
    v.push_back(34);
    v.push_back(23);
    for(int i=0;i<v.size();i++)
    {
        cout<<v[i]<<endl;
    }
    v.pop_front();
    v.pop_back();
    //pop_back();
    for(int i=0;i<v.size();i++)
    {
        cout<<v[i]<<endl;
    }
}
