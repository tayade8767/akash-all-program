#include <iostream>
#define max 100
using namespace std;
int main() 
{
   int a[max];
   int *ptr=a;

   cout<<"enter the string"<<endl;
   for(int i=0;i<3;i++)
   {
   cin>>a[i];
   }
   //while(*(ptr++)!='\0')count++;
    
    for(int i=0;i<3;i++)
   {
  cout<<*(ptr++);
   }
    return 0;
}
