#include <iostream>
#define max 100
using namespace std;
int main() 
{
   char a[max];
   char *ptr=a;
   int count=0;
   cout<<"enter the string"<<endl;
   cin>>a;
   while(*(ptr++)!='\0')count++;
    cout<<"total length of an array::"<<count;
    return 0;
}
