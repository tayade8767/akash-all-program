#include<iostream>
#include<cmath>
using namespace std;
int Recursive_Sum(int n)
{
    if(n>0)
    {
        return Recursive_Sum(n-1)+n;
    }
}
int main()
{
    int n;
    cout<<"Enter the number"<<endl;
    cin>>n;
    cout<<Recursive_Sum(n);
}
